# 依赖定义在 pyproject.toml 中，此文件用于 pip install -r 快速安装
# P3 安全修复：使用 ~= 约束（兼容版本），允许 patch 升级但锁定 minor 版本
# 版本约束与 requirements.lock 中实际锁定的版本对齐
# 生产环境应使用 requirements.lock（带哈希校验）完全固定依赖
# 运行时依赖（与 pyproject.toml [project].dependencies 保持同步）
langchain~=1.3.0
langchain-openai~=1.3.0
langchain-chroma~=1.1.0
langchain-text-splitters~=1.1.0
langgraph~=1.2.0
langgraph-checkpoint-sqlite~=3.1.0
chromadb~=1.5.0
python-dotenv~=1.2.0
streamlit~=1.58.0
tavily-python~=0.7.0
pydantic~=2.13.0
pydantic-settings~=2.14.0
fastapi~=0.139.0
uvicorn~=0.49.0
tenacity~=9.1.0
pypdf~=6.14.0
requests~=2.34.0
cachetools~=7.1.0
